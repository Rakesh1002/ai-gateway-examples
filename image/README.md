# image — one-shot generation

```sh
cp .env.example .env
pnpm install
pnpm start
# wrote out.png
```

Models to try: `black-forest-labs/flux-1-schnell` (fast), `stabilityai/sd-3.5-large` (detail), `google/imagen-4` (photoreal).
